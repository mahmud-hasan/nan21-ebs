create table BUDGET
(
  ID        NUMBER(10) not null,
  CLIENT_ID NUMBER(10)
)
;
comment on table BUDGET
  is 'Budgets (Code: BUDGET)';
alter table BUDGET
  add constraint PK_BUDGET primary key (ID);

