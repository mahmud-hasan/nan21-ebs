create table SUBSCRIPTION
(
  ID        NUMBER(10) not null,
  CODE      VARCHAR2(16),
  NAME      VARCHAR2(255),
  CLIENT_ID NUMBER(10)
)
;
comment on table SUBSCRIPTION
  is 'Subscriptions (Code: SUBSC)';
alter table SUBSCRIPTION
  add constraint PK_SUBSC primary key (ID);

